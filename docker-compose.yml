version: "3.8"

services:
  api:
    build: .
    container_name: memoai-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Absolute paths inside the container; volume below persists them.
      DATABASE_URL: sqlite+aiosqlite:////data/memoai.db
      STORAGE_DIR: /data/uploads
    volumes:
      - ./data:/data
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/docs')",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s